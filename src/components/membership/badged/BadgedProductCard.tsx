// Superseded — the "Badged Version" dashboard now layers badges onto the real
// Marketing Focused layout's cards via `CardBadgeOverlay` rather than rendering a
// dedicated card. Kept as an empty module (the workspace mount disallows delete).
export {}
